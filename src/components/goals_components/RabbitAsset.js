import React from 'react'
import styled from '@emotion/styled'

const RabbitAsset = ({height, width, aspectRatio}) => {
    const RabbitAssetStyle = styled.div`
      top: 343px;
      left: 1505px;
      width: 54px;
      height: 49px;
      transform: matrix(-0.98, -0.19, 0.19, -0.98, 0, 0);
    `;

    return(
        
        <text>Ho</text>
        
    );
}

export default RabbitAsset;